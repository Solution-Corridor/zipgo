CREATE TABLE `expert_rates` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `expert_id` bigint(20) UNSIGNED NOT NULL COMMENT 'References expert_details.id',
  `service_name` varchar(100) NOT NULL,
  `rate` decimal(10,2) NOT NULL,
  `description` text DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `expert_rates_expert_id_foreign` (`expert_id`),
  CONSTRAINT `expert_rates_expert_id_foreign` FOREIGN KEY (`expert_id`) REFERENCES `expert_details` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;


ALTER TABLE `expert_rates` CHANGE `rate` `rate` INT(11) NOT NULL;

ALTER TABLE `expert_rates` 
ADD COLUMN `image` varchar(255) DEFAULT NULL AFTER `description`;